# Cargo build artifacts
/target
**/*.rs.bk
Cargo.lock.bak

# Local-only scratch space (per-developer experimentation, not tracked)
.scratch/

# Security: prevent accidental commits of local GPG passphrases
*gpg-passphrase*
