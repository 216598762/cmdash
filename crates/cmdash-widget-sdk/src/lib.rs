//! c-ABI-safe `CmdashWidget` trait plus a pinned ABI version, for
//! dynamic `.so` / `.dll` widgets loaded via `libloading`.
//!
//! Scaffolding stub. See `AGENTS.md` §"Plugin model — native Rust widgets".
