//! cmdash binary entry point.
//!
//! Scaffolding stub. Real implementation flows through the
//! `cmdash` library crate; the binary is intentionally thin.

fn main() {
    cmdash::_cmdash_dep_smoke();
}
